import React from 'react';
import Hero from '../components/Hero';

const Landing: React.FC = () => {
    return (
        <main className="">
            <Hero />
        </main>
    );
};

export default Landing;